# TEST ONLY. Copy to .env in this directory (git-ignored) before running the stack.
POSTGRES_DB=agenda
POSTGRES_USER=agenda
POSTGRES_PASSWORD=proxytest-db-password
DJANGO_SECRET_KEY=proxytest-insecure-secret-key-change-nothing-real-uses-it
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://localhost:18443
DJANGO_SCRIPT_NAME=/agenda
