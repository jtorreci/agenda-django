# Copy to .env.garnocex on Garnocex. Never commit the populated file.
POSTGRES_DB=agenda
POSTGRES_USER=agenda
POSTGRES_PASSWORD=
DJANGO_SECRET_KEY=
ALLOWED_HOSTS=
CSRF_TRUSTED_ORIGINS=
# Optional: port exposed only on the server loopback interface.
APP_PORT=8000
